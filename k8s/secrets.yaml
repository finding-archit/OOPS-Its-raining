
# DO NOT store actual secret values here.
# Create the real secret by running:
#
#   kubectl create secret generic weather-secrets \
#     --from-literal=api-key=YOUR_ACTUAL_OPENWEATHER_API_KEY

apiVersion: v1
kind: Secret
metadata:
  name: weather-secrets
  namespace: default
type: Opaque
